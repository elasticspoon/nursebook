<%= form_with(model: friendship) do |form| %>
  <% if friendship.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(friendship.errors.count, "error") %> prohibited this friendship from being saved:</h2>

      <ul>
        <% friendship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_one_id, style: "display: block" %>
    <%= form.text_field :user_one_id %>
  </div>

  <div>
    <%= form.label :user_two_id, style: "display: block" %>
    <%= form.text_field :user_two_id %>
  </div>

  <div>
    <%= form.label :friends_count, style: "display: block" %>
    <%= form.number_field :friends_count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
