<%# <%= turbo_frame_tag id=dom_id(:liked_post, @post) do %> %>
  <%= turbo_frame_tag id='testing' do %>
    <%= button_to 'Like', [:liked_posts], { 
    class: 'post__social-button', form_class: 'post__social-button',
    form: { data: { button_modifier_target: 'form'}},
    params: { liked_post: { post_id: @post.id }}, 
    data: { 
      action: 'click->button-modifier#likeObject', 
      button_modifier_target: 'button'
    }
  } %>
  <% end %>
