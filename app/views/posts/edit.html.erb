<h1>Editing post</h1>
<%= render (PostComponent.new(post: @post, current_user:) do
  render("form", post: @post)
  end ) %>
<br>
<div>
  <%= link_to "Show this post", @post %> |
  <%= link_to "Back to posts", posts_path %>
</div>
